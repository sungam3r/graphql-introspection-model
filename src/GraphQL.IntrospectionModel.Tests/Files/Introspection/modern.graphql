# test
directive @my(arg: String!) on
  | FIELD_DEFINITION

schema {
  query: Query
  mutation: Mutation
  subscription: Subscription
}

type Mutation {
  print: Boolean
}

input Person {
  age: Int!
}

type Query {
  hello: String @my(arg: "value")
}

type Subscription {
  values: [Int]
}
